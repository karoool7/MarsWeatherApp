spring:
  datasource:
    url: jdbc:h2:file:./db/marsdb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

nasa-client:
  url: https://mars.nasa.gov/rss/api/?feed=weather&category=msl&feedtype=json